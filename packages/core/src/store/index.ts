export { slice } from './slice.js';
export { reconcile, unwrap, produce } from './utilities.js';

import { slice } from './slice.js';
import { reconcile, unwrap, produce } from './utilities.js';


export const store = {
    slice,
    reconcile,
    unwrap,
    produce
};
