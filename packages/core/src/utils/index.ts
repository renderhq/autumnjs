export { merge, mergeProps } from './props.js';
export { split, splitProps } from './split.js';
export { resolve, children } from './children.js';
export { context, createContext, useContext } from './context.js';
export { uid, createUniqueId } from './id.js';

import { merge, mergeProps } from './props.js';
import { split, splitProps } from './split.js';
import { resolve, children } from './children.js';
import { context, createContext, useContext } from './context.js';
import { uid, createUniqueId } from './id.js';

export const utils = {
    merge,
    mergeProps,
    split,
    splitProps,
    resolve,
    children,
    context,
    createContext,
    useContext,
    uid,
    createUniqueId
};
